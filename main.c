#include "philophers.h"

int	main(int ac, char **av)
{
	t_table	table;

	if (ac != 5 || ac != 6)
		exit_error(USAGE_ERROR);
	//parse args
    set_the_table(&table, av);
	//data init
    
	//dinner_start
	//clean
	return (0);
}
